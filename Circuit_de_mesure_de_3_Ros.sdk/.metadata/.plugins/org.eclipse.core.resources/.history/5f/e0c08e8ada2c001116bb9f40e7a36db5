#ifndef MAIN_H_
#define MAIN_H_

#include <stdio.h>
#include <string.h>
#include "platform.h"
#include "xparameters.h"
#include "xil_io.h"
#include "xtime_l.h"
#include "sleep.h"

/* Module SD (contient les variables freq_all, etc.) */
#include "sd_logger.h"

/* --- CONFIGURATION HARDWARE IP CAPTURE --- */
// Adresse de base définie dans Vivado
#define BASE_ADDR              XPAR_IP_CONVERSION_AXI_0_S00_AXI_BASEADDR

// Offsets basés sur tes slv_reg VHDL
#define REG_DATA_ALLOW_OFFSET  0    // slv_reg0 : Data (8 bits) et bit Allow (bit 8)
#define REG_DONE_OFFSET        8    // slv_reg2 : Signal Done pour valider l'octet
#define MASK_ALLOW             0x100 // Masque pour isoler le bit 8 (2^8 = 256 ou 0x100)

/* --- PARAMÈTRES DE CYCLE --- */
#define PAUSE_THERMIQUE_SEC    300

/* Prototypes des fonctions du main.c */
void system_init(void);

#endif /* MAIN_H_ */